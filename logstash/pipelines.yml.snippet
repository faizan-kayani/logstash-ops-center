# --- Register the pipeline in pipelines.yml ---
- pipeline.id: error-monitoring
  path.config: "/etc/logstash/conf.d/error-monitoring.conf"
  pipeline.workers: 4
  pipeline.batch.size: 250
  queue.type: persisted
  dead_letter_queue.enable: true
